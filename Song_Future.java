import greenfoot.*;
import java.awt.Color;
/**
 * Write a description of class Song_Future here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Song_Future extends Actor
{
    /**
     * Act - do whatever the Song_Future wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public Song_Future(){
        setImage(new GreenfootImage("Back - On Where is the future", 20, Color.WHITE, Color.BLACK));
    }
}
